# C语言入门
## PART 1
1. 
- GCC：，一个编译器系统，将c语言的代码翻译为计算机语言
- MinGW:一个将GCC等工具移植到windows系统上的工具，让windows用上GCC
2. 
- c_cpp文件用于配置代码的智能感知
- task文件用于配置编译任务
- launch文件用于配置调试环境
3. 
- 在vscode上下载插件可以让vscode理解编写和运行c语言
- 插件的作用是语法高亮和代码美化，智能感知，指出错误等
4. 
- [图片]("D:\GLIMMER\5_B9RO~E`D00IRDZBM6MT@0.png")
## PART 2
1. 
- 变量的类型：变量被指定的类型，规定变量该如何被存储和使用,决定了分配多少内存空间，以及如何解释这块内存中的二进制数据。
- 常用的变量类型：整型char,short,long,long long,浮点型float，double，float double，逻辑型bool，指针，自定义变量
- 存放年龄应该使用整数int
- 不能实现，char通常储存单个字符，不适用于apple。正确做法应是采用char*指针的形式来存放。
1. 
- 第一个元素的下标通常是从0开始的
- 越界的问题是读到垃圾值，覆盖其他对象，立即导致崩溃或暂时正常
- 危险的原因编译器不报错导致难以察觉，且容易造成安全漏洞
1. 
- `for (i=1;i<=n;i++){n/10}`
- `while(i<=n){i++,n/10}`
-  `do{}while`
- 初始化在循环前执行一次
  条件判断用于进入循环和结束循环
  迭代部分用于更新变量，及时走出循环
- 见.c文件
1. 
- 条件中的运算对象是布尔值，用于判断对错，算式中的运算对象是数值，用于计算，运算结果前者判断是否进入循环，后者计算出答案并且存储
- &&代表两个条件均满足才进入循环，||表示或，只要有一个满足条件即可，！代表取反
## PART 3
1. a和b的数值没交换，设定的函数中的a，b与运行时的a，b完全不一样
2. 修改方案函数中改为*a，*b，运用指针的方式才能正确的传递数据
## 收获
单单学习部分就花了好几天，哪怕学过做题时也时常卡住，但解出来的时候无疑是兴奋的，只能说微光招新是一场特训，受益匪浅吧