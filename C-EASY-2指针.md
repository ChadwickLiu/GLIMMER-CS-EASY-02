# C-EASY-2：指针
## STEP 1
1. 
- 通过`*p`的形式来定义指针变量，例如`int *p`,`char *p`等等
- 指针变量的大小是固定的，它的大小取决于系统架构，即cpu位数
2. 
- 结果是20 10
- 运算的优先级导致y的输出结果变成了10，优先级为后缀>前缀，因此++*arr表示的是++(*arr)，即++arr[0]，因此前者等于4，而*++arr，代表的则是*arr[1],因此后者等于6，加起来是10
- 野指针是指指向未知内存地址的指针，它指向一个随机的非法的，或已经失去访问权限的内存地址
- 危害可能导致程序崩溃，数据被篡改，安全漏洞，内存泄漏等问题
- 定义时先进行初始化，将其初始化为null，使用指针之前将其赋值绑定给一个可用地址，在指针解引用之前，先去判断这个指针是不是null
3. 
- 结构体是一种由不同数据类型成员组成的复合数据类型
- 结构体指针是指存放结构体地址的变量，能直接操作内存数据
- 结构体的内存对齐规则：成员偏移量是对齐数的整倍数，结构体总大小是最大偏移量的整数倍，对齐数取编译器默认值与成员大小的较小值

## STEP 2
1. 
- 数组在内存中是一块连续的存储空间，而链表靠指针链接  
  数组的大小固定，而链表大小易于改变
  数组可以直接提取数据，而链表只能从head开始挨个查看
  增删数据时数组牵一发而动全身，链表则调节起来较为轻松
2. 
- 结构特点：使用结构体加结构体指针的形式将一堆结构体给串接起来  
            一般情况下需要定义head，p，tail三种指针，然后通过next将结构体串接在一起，形成一种链条的结构